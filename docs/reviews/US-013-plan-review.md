## Ревью плана: US-013 «Локация Лес/Природа»

### Вердикт: Готов к реализации

### Соответствие архитектуре (project_state.md)

- План соблюдает контракт локации из `project_state.md`: `init(container, w, h), onBeat(phase), onHit(zone), destroy()`.
- Явно учтён паттерн подключения новой локации из `project_state.md`: `register('forest', 'Лес', createForestLocation)` + side-effect import в `Animator.js`.
- Подход на `PIXI.Graphics` без внешних ассетов согласован с текущим стеком и ограничениями боевого окружения (Windows 7 + Chrome).
- Добавлен отдельный блок производительности (лимиты эффектов, отсутствие аллокаций в `onBeat()`), что напрямую покрывает известный риск WebGL из `project_state.md`.

### Пробелы в плане

- Критичных пробелов не обнаружено.
- Небольшое уточнение: в пункте проверки через консоль стоит зафиксировать конкретный способ доступа к `animator` (например, временно `window.__animator = animator` в dev-режиме), чтобы QA-шаг был полностью воспроизводим.

### Конфликты с существующей архитектурой

- Конфликтов не обнаружено.

### Рекомендации для доработки

1. Перед реализацией зафиксировать в плане/таске, как именно открыть доступ к `animator` для детерминированной проверки зон (`window.__animator` в dev-only).
2. После реализации добавить краткий чек в PR: подтверждение лимитов эффектов под нагрузкой (частые нажатия).
3. Сохранить порядок внедрения по слайсам (сначала базовая сцена, затем реакции), как сейчас описано в плане.
