# US-010: Редизайн UI всех экранов

## Контекст

Текущий UI — функциональный, но визуально скучный (белый фон, плоские элементы). Для детей 3-15 лет нужен яркий, игровой, мультяшный стиль. Пользователь выбрал: градиентные фоны, игровой стиль, творческая свобода.

## Ограничения

- **Только HTML + CSS.** JS не трогаем.
- Все ID элементов сохраняются (JS привязан к ним).
- `style="display: none"` на session-screen и stats-screen сохраняется.
- `app.js` переключает экраны через `style.display = 'block'/'none'` → нельзя ставить `display: flex` на обёртки экранов, используем внутренние контейнеры.

## Файлы

- `src/web/index.html` — добавить шрифт, обёртки, эмодзи-иконки
- `src/web/styles/main.css` — полная переработка стилей

## Палитра

| Роль | Цвет | Hex |
|---|---|---|
| Setup фон (старт) | Sky Blue | `#4FC3F7` |
| Setup фон (конец) | Lavender | `#B388FF` |
| Session фон (старт) | Deep Indigo | `#303F9F` |
| Session фон (конец) | Dark Purple | `#4A148C` |
| Stats фон (старт) | Gold | `#FFD54F` |
| Stats фон (конец) | Warm Orange | `#FF8A65` |
| Кнопки (primary) | Green | `#66BB6A` |
| Кнопки (danger) | Coral Red | `#EF5350` |
| Акцент (BPM, фокус) | Electric Blue | `#2979FF` |
| Карточки | White 92% | `rgba(255,255,255,0.92)` |
| Perfect / Good / Miss | `#4CAF50` / `#FFC107` / `#F44336` |

## Шрифт

- **Nunito** (Google Fonts) — круглый, дружелюбный, поддерживает кириллицу
- Веса: 700, 900
- Fallback: `'Segoe UI', Arial, sans-serif`

## Слайсы

### Слайс 1: Фундамент + фоны экранов
- CSS переменные (новая палитра)
- Google Fonts `<link>` в `<head>`
- Body: убрать padding, сбросить фон
- Каждый экран: `min-height: 100vh`, свой градиент, padding
- Обновить h1, h2 — новые размеры, веса

**Тест:** открыть приложение → градиентный фон, новый шрифт, переходы между экранами работают.

### Слайс 2: Setup Screen — карточка + кнопки + контролы
- Обёртка `<div class="card">` внутри setup-screen
- Карточка: белый полупрозрачный фон, скругления 24px, тень
- Кнопки: скругления 20px, цветные тени, hover-подъём, active-нажатие
- Ползунок BPM: кастомный стиль (цветной трек, большой круглый thumb)
- Поле ввода: скруглённое, фокус с подсветкой
- Эмодзи в лейблах: 🎵 Темп, ⏱️ Время, 🚀 Старт

**Тест:** Setup экран — красивая карточка на градиенте, все контролы работают.

### Слайс 3: Session Screen — минимализм
- Убрать `<h2>Занятие идёт</h2>` (не привязан к JS)
- Обёртка `<div class="session-layout">`
- Таймер: белый, крупный, с текстовой тенью (свечение)
- Canvas: скругления, тень-свечение
- Индикатор метронома: полупрозрачный, неброский
- Кнопка Стоп: 🛑 Стоп

**Тест:** Session экран — тёмный, фокус на canvas и таймере, кнопка стоп работает.

### Слайс 4: Stats Screen — праздник
- Обёртка `<div class="stats-layout">`
- Три карточки в ряд (flex row) с цветными верхними бордерами
- Эмодзи-иконки: 🌟 Отлично, 👍 Хорошо, 💨 Мимо
- Заголовок: пульсирующая CSS-анимация
- Декоративные точки через `::before`/`::after`
- Кнопка: 🔄 Новое занятие

**Тест:** Stats экран — золотой фон, карточки с результатами, анимация заголовка.

### Слайс 5: Адаптивность и полировка
- `@media (max-width: 768px)` — карточки статистики в колонку, уменьшить шрифты
- `@media (min-width: 1200px)` — увеличить max-width
- `overflow-x: hidden`, `:focus-visible`

**Тест:** ресайз окна, всё работает на разных разрешениях.

## Верификация

После каждого слайса проверить полный цикл: Setup → Start → Session (таймер тикает, canvas рисует) → Stop → Stats → Новое занятие → Setup. Все данные корректны, ничего не сломано.
