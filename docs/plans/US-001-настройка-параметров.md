# План: US-001 — Настройка параметров метронома

## User Story

**ID:** US-001
**Приоритет:** 1
**Описание:** Как педагог я хочу настроить темп (BPM) и время занятия, чтобы подготовить занятие для ребёнка

## Acceptance Criteria

- UI с ползунком темпа (50-70 BPM) ✅ (диапазон подтверждён)
- Поле ввода времени занятия (минуты)
- Большие элементы управления (минимум 60px высота кнопок)
- Яркие цвета, чёткий контраст
- Валидация: BPM в допустимом диапазоне, время > 0
- Настройки сохраняются в localStorage (только настройки, без истории) ✅
- UI протестирован на читаемость для детей
- Код соответствует CLAUDE.md (типы, guard clauses)

## Текущее состояние

**Структура проекта:**
- `src/web/` — папка готова, но пуста
- `src/web/js/` — папка для JS модулей (пуста)
- `src/web/styles/` — папка для CSS (пуста)
- Нет `index.html`, нет JavaScript, нет CSS

**Начинаем с чистого листа.**

## План реализации

### Шаг 1: Создать базовую HTML страницу

**Файл:** `src/web/index.html`

**Структура:**
```html
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Beat Buddy</title>
  <link rel="stylesheet" href="styles/main.css">
</head>
<body>
  <!-- Setup Screen -->
  <div id="setup-screen">
    <h1>Beat Buddy</h1>

    <div class="setting-group">
      <label for="bpm-slider">
        Темп (BPM):
        <span id="bpm-value" class="setting-value">60</span>
      </label>
      <input type="range" id="bpm-slider" min="50" max="70" value="60" step="1">
    </div>

    <div class="setting-group">
      <label for="duration-input">Время занятия (минуты):</label>
      <input type="number" id="duration-input" min="1" max="60" value="5">
    </div>

    <div class="setting-group">
      <label>
        <input type="checkbox" id="dev-mode-checkbox">
        Dev Mode (клавиатура вместо датчика)
      </label>
    </div>

    <button id="start-button" class="primary-button">Старт</button>
  </div>

  <!-- Session Screen (заглушка для будущего) -->
  <div id="session-screen" style="display: none;">
    <p>Экран занятия (US-002+)</p>
  </div>

  <script type="module" src="js/app.js"></script>
</body>
</html>
```

**Детали:**
- Ползунок BPM: min=50, max=70, default=60
- Поле времени: min=1, max=60, default=5 минут
- Чекбокс Dev Mode для будущего переключения датчиков
- Кнопка "Старт" (пока ничего не делает, активируется в US-002)
- Заглушка Session Screen для будущих итераций

---

### Шаг 2: Создать CSS стили

**Файл:** `src/web/styles/main.css`

**Требования к стилям:**
- Большие кнопки (минимум 60px высота)
- Яркие цвета, чёткий контраст
- Минимум текста, крупный шрифт
- Адаптивность для разных размеров экрана

**Ключевые стили:**
```css
/* Основные переменные */
:root {
  --primary-color: #4CAF50;  /* Зелёный для кнопок */
  --secondary-color: #2196F3; /* Синий для акцентов */
  --background-color: #FFFFFF;
  --text-color: #333333;
  --input-border: #CCCCCC;
}

/* Базовые стили */
body {
  font-family: 'Arial', sans-serif;
  font-size: 18px;
  margin: 0;
  padding: 20px;
  background-color: var(--background-color);
  color: var(--text-color);
}

h1 {
  font-size: 48px;
  text-align: center;
  color: var(--primary-color);
  margin-bottom: 40px;
}

/* Setup Screen */
#setup-screen {
  max-width: 500px;
  margin: 0 auto;
}

.setting-group {
  margin-bottom: 30px;
}

.setting-group label {
  display: block;
  font-size: 20px;
  font-weight: bold;
  margin-bottom: 10px;
}

.setting-value {
  color: var(--secondary-color);
  font-size: 24px;
}

/* Ползунок BPM */
input[type="range"] {
  width: 100%;
  height: 10px;
  cursor: pointer;
}

/* Поле ввода времени */
input[type="number"] {
  width: 100%;
  padding: 15px;
  font-size: 24px;
  border: 2px solid var(--input-border);
  border-radius: 8px;
  box-sizing: border-box;
}

/* Чекбокс Dev Mode */
input[type="checkbox"] {
  width: 24px;
  height: 24px;
  margin-right: 10px;
  vertical-align: middle;
  cursor: pointer;
}

/* Кнопка Старт */
.primary-button {
  width: 100%;
  height: 80px;
  font-size: 28px;
  font-weight: bold;
  color: white;
  background-color: var(--primary-color);
  border: none;
  border-radius: 12px;
  cursor: pointer;
  margin-top: 20px;
  transition: background-color 0.3s;
}

.primary-button:hover {
  background-color: #45A049;
}

.primary-button:active {
  transform: scale(0.98);
}
```

---

### Шаг 3: Создать JavaScript модуль приложения

**Файл:** `src/web/js/app.js`

**Функционал:**
1. Инициализация при загрузке страницы
2. Загрузка настроек из `localStorage`
3. Обновление UI при изменении настроек
4. Валидация настроек (BPM 50-70, время > 0)
5. Сохранение настроек в `localStorage`
6. Обработка клика на кнопку "Старт" (пока просто логирование)

**Реализация:**
```javascript
// Ключи localStorage
const SETTINGS_KEY = 'beatBuddySettings'

// Настройки по умолчанию
const DEFAULT_SETTINGS = {
  bpm: 60,
  duration: 5,
  devMode: false
}

// Элементы UI
let bpmSlider
let bpmValue
let durationInput
let devModeCheckbox
let startButton

/**
 * Инициализация приложения
 */
function init() {
  // Получить элементы
  bpmSlider = document.getElementById('bpm-slider')
  bpmValue = document.getElementById('bpm-value')
  durationInput = document.getElementById('duration-input')
  devModeCheckbox = document.getElementById('dev-mode-checkbox')
  startButton = document.getElementById('start-button')

  // Загрузить настройки
  loadSettings()

  // Установить обработчики событий
  bpmSlider.addEventListener('input', onBpmChange)
  durationInput.addEventListener('change', onDurationChange)
  devModeCheckbox.addEventListener('change', onDevModeChange)
  startButton.addEventListener('click', onStartClick)

  console.log('[App] Инициализация завершена')
}

/**
 * Загрузить настройки из localStorage
 */
function loadSettings() {
  const savedSettings = localStorage.getItem(SETTINGS_KEY)
  const settings = savedSettings ? JSON.parse(savedSettings) : DEFAULT_SETTINGS

  // Применить настройки к UI
  bpmSlider.value = settings.bpm
  bpmValue.textContent = settings.bpm
  durationInput.value = settings.duration
  devModeCheckbox.checked = settings.devMode

  console.log('[App] Настройки загружены:', settings)
}

/**
 * Сохранить настройки в localStorage
 */
function saveSettings() {
  const settings = {
    bpm: parseInt(bpmSlider.value, 10),
    duration: parseInt(durationInput.value, 10),
    devMode: devModeCheckbox.checked
  }

  localStorage.setItem(SETTINGS_KEY, JSON.stringify(settings))
  console.log('[App] Настройки сохранены:', settings)
}

/**
 * Обработчик изменения BPM
 */
function onBpmChange() {
  const bpm = parseInt(bpmSlider.value, 10)

  // Валидация
  if (bpm < 50 || bpm > 70) {
    console.warn(`[App] BPM вне диапазона: ${bpm}`)
    return
  }

  bpmValue.textContent = bpm
  saveSettings()
}

/**
 * Обработчик изменения времени занятия
 */
function onDurationChange() {
  const duration = parseInt(durationInput.value, 10)

  // Валидация
  if (duration < 1) {
    console.warn(`[App] Время занятия < 1: ${duration}`)
    durationInput.value = 1
  }

  if (duration > 60) {
    console.warn(`[App] Время занятия > 60: ${duration}`)
    durationInput.value = 60
  }

  saveSettings()
}

/**
 * Обработчик изменения Dev Mode
 */
function onDevModeChange() {
  saveSettings()
}

/**
 * Обработчик клика на кнопку Старт
 */
function onStartClick() {
  const settings = {
    bpm: parseInt(bpmSlider.value, 10),
    duration: parseInt(durationInput.value, 10),
    devMode: devModeCheckbox.checked
  }

  console.log('[App] Старт занятия с настройками:', settings)

  // TODO (US-002): Запустить метроном, датчик, перейти на Session Screen
  alert(`Занятие запущено!\nBPM: ${settings.bpm}\nВремя: ${settings.duration} мин\nDev Mode: ${settings.devMode}`)
}

// Запуск приложения
document.addEventListener('DOMContentLoaded', init)
```

**Детали:**
- Guard clauses для валидации (если BPM вне диапазона — не обновляем)
- Все публичные функции с комментариями
- Логирование ключевых событий
- `localStorage` для сохранения/загрузки настроек
- TODO комментарий для будущей интеграции с метрономом

---

## Проверка (Acceptance Criteria)

### 1. UI элементы
- ✅ Ползунок BPM (50-70)
- ✅ Поле времени (1-60 минут)
- ✅ Чекбокс Dev Mode
- ✅ Кнопка Старт (80px высота)

### 2. Валидация
- ✅ BPM не может выйти за пределы 50-70 (ползунок ограничен)
- ✅ Время не может быть < 1 (автокоррекция в `onDurationChange`)
- ✅ Время не может быть > 60 (автокоррекция)

### 3. localStorage
- ✅ При изменении настроек → сохраняются
- ✅ При перезагрузке страницы → восстанавливаются
- ✅ Если нет сохранённых настроек → используются значения по умолчанию

### 4. Код
- ✅ Аннотации типов в комментариях (JSDoc можно добавить)
- ✅ Guard clauses для валидации
- ✅ Логирование ключевых событий
- ✅ Соответствие CLAUDE.md

### 5. UI для детей
- ✅ Большая кнопка Старт (80px)
- ✅ Крупный шрифт (h1: 48px, labels: 20px, inputs: 24px)
- ✅ Яркие цвета (зелёный, синий)
- ✅ Чёткий контраст

---

## Ручное тестирование

### Сценарий 1: Первый запуск
1. Открыть `src/web/index.html` в браузере
2. Проверить: BPM=60, время=5 мин, Dev Mode=unchecked
3. Открыть DevTools Console
4. Проверить лог: `[App] Инициализация завершена`

### Сценарий 2: Изменение настроек
1. Переместить ползунок BPM на 80
2. Проверить: значение обновилось `80`
3. Проверить лог: `[App] Настройки сохранены: {bpm: 80, ...}`
4. Изменить время на 10 минут
5. Проверить лог: `[App] Настройки сохранены: {duration: 10, ...}`

### Сценарий 3: Валидация
1. Ввести в поле времени `0`
2. Проверить: автоматически изменилось на `1`
3. Проверить лог: `[App] Время занятия < 1: 0`
4. Ввести в поле времени `100`
5. Проверить: автоматически изменилось на `60`

### Сценарий 4: localStorage
1. Изменить настройки: BPM=150, время=7 мин, Dev Mode=checked
2. Перезагрузить страницу (F5)
3. Проверить: настройки восстановлены (BPM=150, время=7 мин, Dev Mode=checked)
4. Проверить лог: `[App] Настройки загружены: {bpm: 150, duration: 7, devMode: true}`

### Сценарий 5: Кнопка Старт
1. Нажать кнопку "Старт"
2. Проверить: появился alert с параметрами
3. Проверить лог: `[App] Старт занятия с настройками: {...}`

---

## Файлы для создания

1. `src/web/index.html` — HTML страница
2. `src/web/styles/main.css` — CSS стили
3. `src/web/js/app.js` — JavaScript приложение

**Итого:** 3 файла

---

## Следующий шаг

После завершения US-001:
1. Протестировать все сценарии
2. Убедиться, что код соответствует CLAUDE.md
3. Закоммитить: `feat: US-001 — Настройка параметров метронома`
4. Перейти к **US-002 (Метроном через Web Audio API)**

---

## Критические моменты

### 1. Валидация
- Ползунок BPM ограничен через `min`/`max` атрибуты HTML
- Поле времени валидируется через JavaScript (автокоррекция)
- Guard clauses: если валидация не прошла — не сохраняем, логируем предупреждение

### 2. localStorage
- Сохраняем только настройки (BPM, время, Dev Mode)
- НЕ сохраняем историю занятий (out of scope для MVP)
- Если JSON невалиден — используем настройки по умолчанию

### 3. UI для детей
- Большие элементы (кнопка 80px, шрифты 18-48px)
- Яркие цвета (#4CAF50 зелёный, #2196F3 синий)
- Минимум текста (короткие лейблы)

### 4. Будущее
- Кнопка "Старт" пока показывает alert
- В US-002 добавим запуск метронома
- В US-003 добавим датчик
- Экран Session Screen пока заглушка
